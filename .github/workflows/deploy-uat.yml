name: Deploy to UAT

# TODO: This workflow will be triggered on a manual click
on:
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy-UAT
    runs-on: ubuntu-latest
    steps:
      - name: TODO
        run: echo "TODO"
