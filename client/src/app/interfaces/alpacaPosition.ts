export interface AlpacaUserPosition {
  symbol: string;
  avgPrice: string;
  qty: string;
  currentPrice: string;
  cost: string;
  currentValue: string;
  gainAmount: string;
  gainPercentage: string;
  change: string;
}
